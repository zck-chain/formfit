# FormFit 后端依赖
# 开发环境 Python 3.14，因此使用较新的版本以获取预编译 wheel

fastapi>=0.115.6
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20          # 文件上传（图片评估）
jinja2>=3.1.5                      # 管理后台模板
itsdangerous>=2.2.0               # 后台 session

# 数据库
SQLAlchemy>=2.0.36
alembic>=1.14.0

# 鉴权与安全
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic-settings>=2.7.1
pydantic>=2.11.0
email-validator>=2.2.0

# HTTP 客户端（调用 DeepSeek / Qwen-VL）
httpx>=0.28.1

# 工具
python-dotenv>=1.0.1
Pillow>=11.2.0                      # 上传图片校验/处理
